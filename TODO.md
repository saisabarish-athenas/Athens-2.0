# TODO: Fix Task Management module (ERGON)

- [x] Step 1: Inspect current TaskManagementPage UI and identify all broken buttons/actions.

- [ ] Step 2: Implement API-backed task list with loading/error states.
- [ ] Step 3: Add KPI cards computed from live tasks and auto-refresh after mutations.
- [ ] Step 4: Implement New Task modal/form (required fields) using `ergonApi.createTask`.
- [ ] Step 5: Implement Edit task modal/form prefilled from selected task using `ergonApi.updateTask`.
- [ ] Step 6: Implement Delete action with confirmation using `ergonApi.deleteTask`.
- [ ] Step 7: Implement status workflow transitions (To Do → In Progress → Completed) and block invalid transitions.
- [ ] Step 8: Implement overdue detection/highlight and overdue filtering.
- [ ] Step 9: Wire search + filters (status, priority, project_id) to backend query params.
- [ ] Step 10: Add notifications on create/update/complete via NotificationsContext (if available in UI flow).
- [ ] Step 11: Final UI polish (disabled states, stable modals, toasts).

